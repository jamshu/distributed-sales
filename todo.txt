export the retail point ids to generate shards
optimize the fiedlds and remove deuplicate fields on the db
day close summary generate
supervisor ctl configuration
sale list view and detail view enhancement
url parameter
odoo summary to django view using url parameter


# Step 7: Django initial setup - Migrate Database and Custom Commands
echo "Running Django management commands..."
python $PROJECT_DIR/manage.py create_shard_database
python $PROJECT_DIR/manage.py enable_timescale
python $PROJECT_DIR/manage.py migrate
python $PROJECT_DIR/manage.py migrate_shards